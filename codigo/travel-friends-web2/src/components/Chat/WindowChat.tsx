import React from 'react';
import { styles } from './Chat.styled';

const WindowChat = (props: any) => {
    return(
        <div style={{
            // ...styles.supportWindow,
            // ...{ opacity: props.visible ? 1 : 0}
        }}>
        </div>
    );
}

export default WindowChat;